<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:weightSum="1"
    android:layout_marginLeft="10dp"
    android:layout_marginRight="10dp"
    android:layout_marginTop="20dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Unsubscribe player:"
        android:id="@+id/unsubscribeText"
        android:textSize="20dp"
        android:textStyle="bold|italic"
        android:textColor="#333"/>

    <Spinner
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/spinnerPlayerUnsubscribe"
        android:layout_gravity="center_horizontal"
        android:spinnerMode="dropdown"
        android:nestedScrollingEnabled="false" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Sign Player:"
        android:id="@+id/textView2"
        android:textSize="20dp"
        android:textStyle="bold|italic"
        android:textColor="#333"/>

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/nameplayerSign"
        android:layout_gravity="center_horizontal"
        android:inputType="textPersonName"
        android:hint="Name of new player"
        android:layout_marginTop="10dp"
        />


    <Button
        android:id="@+id/signButton"

        android:text="Sign"
        android:textColor="#FFFFFF"
        android:textSize="15sp"

        android:layout_width="151dp"
        android:layout_height="60dp"
        android:background="@drawable/buttonshape"
        android:shadowColor="#000000"
        android:shadowRadius="5"
        android:onClick="onSignPlayer"
        android:layout_marginTop="20dp"
        />

</LinearLayout>